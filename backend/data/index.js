const accountData = require("./account");
const forumData = require("./forum");
const toDoData = require("./toDo")

module.exports = {
  users: accountData,
  forum: forumData,
  toDoData: toDoData
}
